description = "Draft a tailored cover letter from the master profile and a job description."

prompt = """
The full command spec follows. The literal token `$ARGUMENTS` inside the spec refers to whatever the user typed after the command name; treat it as: {{args}}

--- BEGIN SPEC (.claude/commands/cover-letter.md) ---
!{cat .claude/commands/cover-letter.md}
--- END SPEC ---
"""
